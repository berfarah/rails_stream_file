require "spec_helper"

describe Rails::StreamFile do
  it "has a version number" do
    expect(Rails::StreamFile::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
